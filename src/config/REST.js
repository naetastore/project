/**
 * Untuk url (uniform resource locator) selalu gunakan "/" di akhir
 */

const server = {
    // online
    // 'url': 'https://naeta-rest-server.000webhostapp.com/',

    // local development
    'url': 'http://localhost/naeta-rest-server/',

    'apikey': {
        'name': 'apikey',
        'key': 'andinaeta4328'
    }
}
export const REST = { server }